const Store={KEY_PREFIX:'cosmos_',get(key,def=null){try{const v=localStorage.getItem(this.KEY_PREFIX+key);return v?JSON.parse(v):def;}catch(e){return def;}},set(key,val){localStorage.setItem(this.KEY_PREFIX+key,JSON.stringify(val));},remove(key){localStorage.removeItem(this.KEY_PREFIX+key);},getByDate(key,dateStr){const all=this.get(key,{});return all[dateStr]||null;},setByDate(key,dateStr,val){const all=this.get(key,{});all[dateStr]=val;this.set(key,all);},getRange(key,startDate,endDate){const all=this.get(key,{});const result={};Object.keys(all).forEach(d=>{if(d>=startDate&&d<=endDate)result[d]=all[d];});return result;}};
const DateUtil={today(){const d=new Date();return this.format(d);},format(d){const y=d.getFullYear();const m=String(d.getMonth()+1).padStart(2,'0');const day=String(d.getDate()).padStart(2,'0');return `${y}-${m}-${day}`;},parse(str){const[y,m,d]=str.split('-').map(Number);return new Date(y,m-1,d);},weekday(d){const wd=['日','一','二','三','四','五','六'];return '周'+wd[d.getDay()];},weekStart(d=new Date()){const date=new Date(d);const day=date.getDay()||7;date.setDate(date.getDate()-day+1);date.setHours(0,0,0,0);return date;},weekDates(d=new Date()){const ws=this.weekStart(d);const dates=[];for(let i=0;i<7;i++){const dd=new Date(ws);dd.setDate(dd.getDate()+i);dates.push(this.format(dd));}return dates;},weekKey(d=new Date()){const ws=this.weekStart(d);const y=ws.getFullYear();const jan1=new Date(y,0,1);const weekNum=Math.ceil(((ws-jan1)/86400000+jan1.getDay()+1)/7);return `${y}-W${String(weekNum).padStart(2,'0')}`;},weekRangeLabel(d=new Date()){const ws=this.weekStart(d);const we=new Date(ws);we.setDate(we.getDate()+6);return `${this.format(ws)} ~ ${this.format(we)}`;},diffDays(dateStr1,dateStr2){const d1=this.parse(dateStr1);const d2=this.parse(dateStr2);return Math.round((d2-d1)/86400000);},addDays(dateStr,n){const d=this.parse(dateStr);d.setDate(d.getDate()+n);return this.format(d);},streak(key){let streak=0;let dateStr=this.today();while(true){const data=Store.getByDate(key,dateStr);if(data){streak++;dateStr=this.addDays(dateStr,-1);}else{break;}}return streak;}};
const QUOTES=["你不必完美，你只需要真实地在路上。","今天的努力，是未来某个时刻的底气。","慢慢来，比较快。每一次专注都是在靠近自己想去的地方。","宇宙很大，但此刻你正在做的事情很重要。","允许自己有状态不好的时候，那也是成长的一部分。","你走的每一步，都在成为更好的自己。","不必和别人比，你的节奏就是最好的节奏。","今天比昨天多懂一点点，就够了。","保持稳定的节奏，比偶尔的爆发更有力量。","你正在做的事情很难，但你已经在做了，这本身就很了不起。","累了就休息一下，休息也是学习的一部分。","别着急看到结果，种子发芽需要时间。","你不需要每天都有大进步，小步走也会到达。","承认困难，然后继续往前走。","你的努力不会被辜负，只是有时候回报来得晚一些。","今天的你，已经比昨天的你多知道一些了。","做宇宙的主人，从管理好今天开始。","不和别人比较，只和昨天的自己比较。","每一个微小的坚持，都在塑造未来的你。","状态不好的时候，降低标准，但不要停下。","你值得拥有一个更好的未来，所以现在才要好好努力。","把注意力放在能控制的事情上：今天的计划、此刻的专注。","考研是一场马拉松，稳住心态比冲刺更重要。","相信过程，结果不会太差。","你不需要时刻保持高效，你只需要保持在场。","今天的疲惫，是为了明天能走得更远。","给自己一点耐心，也给时间一点时间。","你正在成为自己宇宙的主人，每一步都算数。","即使今天只完成了一件事，那也是前进。","不要害怕慢，要害怕停下来。","你比自己想象的更有韧性。","把每一天过好，未来自然会来。","今天的专注，会成为明天的从容。","允许偶尔的迷茫，但不要在迷茫中停留太久。","你的坚持，终将被看见。","做好今天该做的事，未来会自己展开。","不必焦虑未来，把当下这一步走稳。","成长是悄无声息的，但每一天都在发生。","你正在书写自己的故事，每一页都很重要。","累了就抬头看看星空，你也是宇宙中发光的存在。","保持温柔，也保持力量。","今天的你，很棒。明天继续。","不必追求完美，完成本身就是一种胜利。","你正在做的事情，值得被认真对待。","一步一个脚印，终会抵达。","把大目标拆小，把小事做好。","你不需要一下子变强，只需要每天强一点点。","相信自己的节奏，它正在带你走向对的地方。","今天也辛苦了，好好休息，明天继续。"];
function getDailyQuote(){const today=DateUtil.today();const used=Store.get('used_quotes',[]);const lastQuoteDate=Store.get('last_quote_date','');let quoteIdx=Store.get('last_quote_idx',-1);if(lastQuoteDate!==today){let available=QUOTES.map((_,i)=>i).filter(i=>!used.includes(i));if(available.length===0){available=QUOTES.map((_,i)=>i);Store.set('used_quotes',[]);}quoteIdx=available[Math.floor(Math.random()*available.length)];const newUsed=[...used,quoteIdx];Store.set('used_quotes',newUsed);Store.set('last_quote_date',today);Store.set('last_quote_idx',quoteIdx);}return QUOTES[quoteIdx]||QUOTES[0];}
const NAV_ITEMS=[{id:'dashboard',icon:'🏠',label:'首页',title:'🪐 今日宇宙',subtitle:'欢迎回到你的星空'},{id:'tasks',icon:'🪐',label:'宇宙任务',title:'🪐 今日宇宙任务',subtitle:'今天也要成为更好的自己'},{id:'math',icon:'📚',label:'数学星球',title:'📚 数学星球',subtitle:'按章节管理你的数学宇宙'},{id:'english',icon:'📖',label:'英语星球',title:'📖 英语星球',subtitle:'英语学习的长期积累'},{id:'mistakes',icon:'📝',label:'错题宇宙',title:'📝 错题宇宙',subtitle:'每道错题都是成长的星标'},{id:'timeline',icon:'📈',label:'学习轨迹',title:'📈 学习轨迹',subtitle:'记录每一次专注的旅程'},{id:'health',icon:'🌱',label:'健康能量',title:'🌱 健康能量站',subtitle:'照顾好身体，才能走得更远'},{id:'exercise',icon:'🏃',label:'运动记录',title:'🏃 运动记录',subtitle:'运动让宇宙更有能量'},{id:'diet',icon:'🍓',label:'饮食记录',title:'🍓 饮食记录',subtitle:'好好吃饭，也是爱自己'},{id:'daily',icon:'🌙',label:'每日复盘',title:'🌙 每日复盘',subtitle:'今天的你，做得怎么样'},{id:'weekly',icon:'🌌',label:'每周复盘',title:'🌌 每周复盘',subtitle:'一周回望，继续前行'}];
let currentPage='dashboard';
function $(sel,parent=document){return parent.querySelector(sel);}
function $$(sel,parent=document){return[...parent.querySelectorAll(sel)];}
function toast(msg,duration=2200){const t=$('#toast');t.textContent=msg;t.classList.add('show');clearTimeout(t._timer);t._timer=setTimeout(()=>t.classList.remove('show'),duration);}
function showModal(html){$('#modalContent').innerHTML=html;$('#modalOverlay').classList.add('show');if(window.twemoji){try{twemoji.parse($('#modalContent'),{folder:'svg',ext:'.svg',base:'https://cdn.jsdelivr.net/gh/twitter/twemoji@v14.0.2/assets/'});}catch(e){}}}
function closeModal(){$('#modalOverlay').classList.remove('show');}
function escapeHtml(str){if(str==null)return'';return String(str).replace(/&/g,'&').replace(/</g,'<').replace(/>/g,'>').replace(/"/g,'"');}
const PRIORITIES=[{key:'high',label:'⭐ 高',cls:'priority-high'},{key:'mid',label:'✨ 中',cls:'priority-mid'},{key:'low',label:'💫 低',cls:'priority-low'}];
function handleImageUpload(file,callback){if(!file)return;const reader=new FileReader();reader.onload=e=>{const img=new Image();img.onload=()=>{const canvas=document.createElement('canvas');let{width,height}=img;const maxW=800;if(width>maxW){height=height*(maxW/width);width=maxW;}canvas.width=width;canvas.height=height;const ctx=canvas.getContext('2d');ctx.drawImage(img,0,0,width,height);const compressed=canvas.toDataURL('image/jpeg',0.7);callback(compressed);};img.src=e.target.result;};reader.readAsDataURL(file);}
function generateStars(){const bg=$('#starsBg');for(let i=0;i<30;i++){const star=document.createElement('div');star.className='star';const size=Math.random()*2+1;star.style.width=size+'px';star.style.height=size+'px';star.style.left=Math.random()*100+'%';star.style.top=Math.random()*100+'%';star.style.animationDelay=Math.random()*4+'s';star.style.animationDuration=(3+Math.random()*3)+'s';bg.appendChild(star);}}
function renderNav(){const list=$('#navList');list.innerHTML=NAV_ITEMS.map(item=>`<li class="nav-item ${item.id===currentPage?'active':''}" data-page="${item.id}"><span class="nav-icon">${item.icon}</span><span class="nav-label">${item.label}</span></li>`).join('');$$('.nav-item').forEach(el=>{el.addEventListener('click',()=>navigateTo(el.dataset.page));});}
function navigateTo(pageId){currentPage=pageId;$$('.nav-item').forEach(el=>{el.classList.toggle('active',el.dataset.page===pageId);});renderPage(pageId);$('#main').scrollTop=0;}
function renderPage(pageId){const item=NAV_ITEMS.find(n=>n.id===pageId);let html=`<div class="page-header"><h1 class="page-title">${item.title}</h1><p class="page-subtitle">${item.subtitle}</p></div>`;switch(pageId){case'dashboard':html+=renderDashboard();break;case'tasks':html+=renderTasks();break;case'math':html+=renderMath();break;case'english':html+=renderEnglish();break;case'mistakes':html+=renderMistakes();break;case'timeline':html+=renderTimeline();break;case'health':html+=renderHealth();break;case'exercise':html+=renderExercise();break;case'diet':html+=renderDiet();break;case'daily':html+=renderDaily();break;case'weekly':html+=renderWeekly();break;}$('#main').innerHTML=`<div class="page active">${html}</div>`;if(window.twemoji){try{twemoji.parse($('#main'),{folder:'svg',ext:'.svg',base:'https://cdn.jsdelivr.net/gh/twitter/twemoji@v14.0.2/assets/'});}catch(e){}}bindPageEvents(pageId);}
function renderDashboard(){const today=DateUtil.today();const now=new Date();const dateStr=`${now.getFullYear()}年${now.getMonth()+1}月${now.getDate()}日 ${DateUtil.weekday(now)}`;const examDate=Store.get('exam_date','2026-12-19');const daysLeft=DateUtil.diffDays(today,examDate);const tasks=Store.getByDate('tasks',today)||getDefaultTasks();const doneCount=tasks.filter(t=>t.done).length;const completionRate=tasks.length>0?Math.round(doneCount/tasks.length*100):0;const timelines=Store.getByDate('timelines',today)||[];const studyMinutes=timelines.reduce((sum,t)=>sum+(t.duration||0),0);const studyHours=(studyMinutes/60).toFixed(1);const mathProgress=calcSubjectProgress('math');const engProgress=calcSubjectProgress('english');const diet=Store.getByDate('diet',today)||{};const water=diet.water||0;const weekDates=DateUtil.weekDates();let exerciseCount=0;weekDates.forEach(d=>{const ex=Store.getByDate('exercise',d);if(ex&&ex.length>0)exerciseCount+=ex.length;});const health=Store.getByDate('health',today)||{};const sleepHours=health.sleepTime&&health.wakeTime?calcSleepHours(health.sleepTime,health.wakeTime):null;const quote=getDailyQuote();const top3=Store.get('top3_'+today,[]);return `<div class="dash-greeting"><div class="dash-date">${dateStr}</div><div class="dash-hello"><span class="sparkle">✨</span> 欢迎回到你的宇宙</div></div><div class="card countdown-card"><div class="countdown-label">考研倒计时</div><div class="countdown-value">${daysLeft>0?daysLeft:(daysLeft===0?'今天':'已过')}</div><div class="countdown-label">${daysLeft>0?'天':(daysLeft===0?'加油！':'辛苦了')}</div><div class="countdown-date-edit" onclick="editExamDate()">考试日期：${examDate} ✏️</div></div>${top3.length>0?`<div class="card"><div class="card-title"><span class="accent-line"></span>🎯 今日最重要的三件事</div><ul class="top3-list">${top3.map((t,i)=>`<li class="top3-item"><span class="top3-num">${i+1}</span><span class="top3-text">${escapeHtml(t)}</span></li>`).join('')}</ul></div>`:`<div class="card"><div class="card-title"><span class="accent-line"></span>🎯 今日最重要的三件事</div><div class="empty-state"><div class="empty-state-icon">🎯</div><div class="empty-state-text">还没有设置今日三件事<br><a href="#" onclick="navigateTo('tasks');return false;" style="color:var(--lav-600);">去设置 →</a></div></div></div>`}<div class="dash-grid"><div class="card stat-card"><div class="stat-icon">📊</div><div class="stat-value">${completionRate}<span class="unit">%</span></div><div class="stat-label">今日完成率</div></div><div class="card stat-card"><div class="stat-icon">⏱</div><div class="stat-value">${studyHours}<span class="unit">h</span></div><div class="stat-label">今日学习时长</div></div><div class="card"><div class="card-title" style="margin-bottom:8px;"><span class="accent-line"></span>📚 数学进度</div><div style="font-size:13px;color:var(--text-3);">${mathProgress.done}/${mathProgress.total} 章节</div><div class="progress-bar"><div class="progress-fill" style="width:${mathProgress.percent}%"></div></div></div><div class="card"><div class="card-title" style="margin-bottom:8px;"><span class="accent-line"></span>📖 英语进度</div><div style="font-size:13px;color:var(--text-3);">${engProgress.text}</div><div class="progress-bar"><div class="progress-fill" style="width:${engProgress.percent}%"></div></div></div><div class="card stat-card"><div class="stat-icon">💧</div><div class="stat-value">${water}<span class="unit">杯</span></div><div class="stat-label">今日饮水</div></div><div class="card stat-card"><div class="stat-icon">💪</div><div class="stat-value">${exerciseCount}<span class="unit">次</span></div><div class="stat-label">本周运动</div></div></div>${sleepHours!==null?`<div class="card stat-card" style="margin-top:0;"><div class="stat-icon">😴</div><div class="stat-value">${sleepHours}<span class="unit">h</span></div><div class="stat-label">昨晚睡眠时长</div></div>`:''}<div class="card quote-card"><div class="quote-text">${escapeHtml(quote)}</div><div class="quote-author">— 给正在努力的你</div></div><div style="text-align:center;padding:8px 0 20px;font-size:11px;color:var(--text-muted);line-height:1.6;">我不是每天机械地完成任务<br>而是在一点一点成为更好的自己<br>做自己宇宙的主人 ✨</div>`;}
function calcSubjectProgress(subject){if(subject==='math'){const chapters=Store.get('math_chapters',[]);const total=chapters.length;const done=chapters.filter(c=>c.mastered).length;const percent=total>0?Math.round(done/total*100):0;return{total,done,percent};}else{const modules=['word','reading','translation','writing','cloze','newtype'];const weekDates=DateUtil.weekDates();let totalDone=0;weekDates.forEach(d=>{const eng=Store.getByDate('english',d)||{};modules.forEach(m=>{if(eng[m]&&eng[m].content)totalDone++;});});const totalPossible=weekDates.length*modules.length;const percent=totalPossible>0?Math.round(totalDone/totalPossible*100):0;return{text:`本周 ${totalDone}/${totalPossible} 模块`,percent};}}
function calcSleepHours(sleepTime,wakeTime){const[sh,sm]=sleepTime.split(':').map(Number);const[wh,wm]=wakeTime.split(':').map(Number);let sleepMin=(wh*60+wm)-(sh*60+sm);if(sleepMin<0)sleepMin+=24*60;return(sleepMin/60).toFixed(1);}
function editExamDate(){const current=Store.get('exam_date','2026-12-19');showModal(`<div class="modal-title">📅 设置考研日期</div><div class="field-group"><label class="field-label">考试日期</label><input type="date" class="input" id="examDateInput" value="${current}"></div><div style="display:flex;gap:10px;margin-top:16px;"><button class="btn btn-ghost btn-block" onclick="closeModal()">取消</button><button class="btn btn-primary btn-block" onclick="saveExamDate()">保存</button></div>`);}
function saveExamDate(){const val=$('#examDateInput').value;if(val){Store.set('exam_date',val);closeModal();toast('考研日期已更新 ✨');renderPage('dashboard');}}
function getDefaultTasks(){return[{id:Date.now()+1,name:'数学学习',note:'',priority:'high',done:false},{id:Date.now()+2,name:'英语学习',note:'',priority:'high',done:false},{id:Date.now()+3,name:'错题整理',note:'',priority:'mid',done:false},{id:Date.now()+4,name:'今日运动',note:'',priority:'mid',done:false},{id:Date.now()+5,name:'今日复盘',note:'',priority:'low',done:false}];}
function renderTasks(){const today=DateUtil.today();let tasks=Store.getByDate('tasks',today);if(!tasks){tasks=getDefaultTasks();Store.setByDate('tasks',today,tasks);}const meta=Store.getByDate('task_meta',today)||{goal:'',reward:'',plannedHours:''};const doneCount=tasks.filter(t=>t.done).length;const rate=tasks.length>0?Math.round(doneCount/tasks.length*100):0;let html=`<div class="card"><div class="card-title"><span class="accent-line"></span>🎯 今日目标</div><div class="field-group"><label class="field-label">今日目标</label><input type="text" class="input" id="taskGoal" placeholder="今天想达成什么？" value="${escapeHtml(meta.goal)}"></div><div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;"><div class="field-group"><label class="field-label">🎁 今日奖励</label><input type="text" class="input" id="taskReward" placeholder="完成后奖励自己..." value="${escapeHtml(meta.reward)}"></div><div class="field-group"><label class="field-label">⏱ 预计学习时长</label><input type="number" class="input" id="taskHours" placeholder="小时" value="${escapeHtml(meta.plannedHours)}"></div></div></div><div class="task-summary"><div class="task-summary-item"><div class="task-summary-label">今日完成率</div><div class="task-summary-value">${rate}%</div><div class="progress-bar" style="margin-top:6px;"><div class="progress-fill" style="width:${rate}%"></div></div></div><div class="task-summary-item"><div class="task-summary-label">完成 / 总数</div><div class="task-summary-value">${doneCount} / ${tasks.length}</div></div></div>`;if(tasks.length===0){html+=`<div class="card"><div class="empty-state"><div class="empty-state-icon">🪐</div><div class="empty-state-text">还没有任务，添加一个开始今天吧</div></div></div>`;}else{html+=tasks.map(t=>{const pri=PRIORITIES.find(p=>p.key===t.priority)||PRIORITIES[1];return `<div class="task-card ${t.done?'done':''}" data-id="${t.id}"><div class="task-checkbox ${t.done?'checked':''}" onclick="toggleTask(${t.id})"></div><div class="task-body"><div class="task-name">${escapeHtml(t.name)}</div>${t.note?`<div class="task-note">📝 ${escapeHtml(t.note)}</div>`:''}<div class="task-meta"><span class="priority-tag ${pri.cls}">${pri.label}</span><button class="btn btn-ghost btn-sm" onclick="editTask(${t.id})">✏️ 编辑</button><button class="btn btn-danger btn-sm" onclick="deleteTask(${t.id})">🗑 删除</button></div></div></div>`;}).join('');}html+=`<button class="btn btn-primary btn-block" onclick="addTaskModal()" style="margin-top:8px;">+ 新增任务</button>`;return html;}
function toggleTask(id){const today=DateUtil.today();const tasks=Store.getByDate('tasks',today)||[];const t=tasks.find(x=>x.id===id);if(t){t.done=!t.done;Store.setByDate('tasks',today,tasks);renderPage('tasks');if(t.done)toast('完成一项！继续加油 ✨');}}
function addTaskModal(){showModal(`<div class="modal-title">新增任务</div><div class="field-group"><label class="field-label">任务名称</label><input type="text" class="input" id="newTaskName" placeholder="例如：复习线性代数"></div><div class="field-group"><label class="field-label">📝 备注</label><textarea class="textarea" id="newTaskNote" placeholder="补充说明..."></textarea></div><div class="field-group"><label class="field-label">⭐ 优先级</label><div class="rating-row" id="priorityPicker">${PRIORITIES.map((p,i)=>`<div class="rating-dot ${i===1?'active':''}" data-pri="${p.key}" style="width:auto;padding:0 12px;">${p.label}</div>`).join('')}</div></div><div style="display:flex;gap:10px;margin-top:16px;"><button class="btn btn-ghost btn-block" onclick="closeModal()">取消</button><button class="btn btn-primary btn-block" onclick="saveNewTask()">添加</button></div>`);let selectedPri='mid';$$('#priorityPicker .rating-dot').forEach(el=>{el.addEventListener('click',()=>{$$('#priorityPicker .rating-dot').forEach(x=>x.classList.remove('active'));el.classList.add('active');selectedPri=el.dataset.pri;});});$('#newTaskName').focus();window._selectedPri=()=>selectedPri;}
function saveNewTask(){const name=$('#newTaskName').value.trim();if(!name){toast('请输入任务名称');return;}const note=$('#newTaskNote').value.trim();const priority=window._selectedPri?window._selectedPri():'mid';const today=DateUtil.today();const tasks=Store.getByDate('tasks',today)||[];tasks.push({id:Date.now(),name,note,priority,done:false});Store.setByDate('tasks',today,tasks);closeModal();toast('任务已添加 ✨');renderPage('tasks');}
function editTask(id){const today=DateUtil.today();const tasks=Store.getByDate('tasks',today)||[];const t=tasks.find(x=>x.id===id);if(!t)return;showModal(`<div class="modal-title">编辑任务</div><div class="field-group"><label class="field-label">任务名称</label><input type="text" class="input" id="editTaskName" value="${escapeHtml(t.name)}"></div><div class="field-group"><label class="field-label">📝 备注</label><textarea class="textarea" id="editTaskNote">${escapeHtml(t.note)}</textarea></div><div class="field-group"><label class="field-label">⭐ 优先级</label><div class="rating-row" id="priorityPicker">${PRIORITIES.map(p=>`<div class="rating-dot ${p.key===t.priority?'active':''}" data-pri="${p.key}" style="width:auto;padding:0 12px;">${p.label}</div>`).join('')}</div></div><div style="display:flex;gap:10px;margin-top:16px;"><button class="btn btn-ghost btn-block" onclick="closeModal()">取消</button><button class="btn btn-primary btn-block" onclick="saveEditTask(${id})">保存</button></div>`);let selectedPri=t.priority;$$('#priorityPicker .rating-dot').forEach(el=>{el.addEventListener('click',()=>{$$('#priorityPicker .rating-dot').forEach(x=>x.classList.remove('active'));el.classList.add('active');selectedPri=el.dataset.pri;});});window._selectedPri=()=>selectedPri;}
function saveEditTask(id){const name=$('#editTaskName').value.trim();if(!name){toast('请输入任务名称');return;}const note=$('#editTaskNote').value.trim();const priority=window._selectedPri?window._selectedPri():'mid';const today=DateUtil.today();const tasks=Store.getByDate('tasks',today)||[];const t=tasks.find(x=>x.id===id);if(t){t.name=name;t.note=note;t.priority=priority;Store.setByDate('tasks',today,tasks);}closeModal();toast('已保存 ✨');renderPage('tasks');}
function deleteTask(id){showModal(`<div class="modal-title">确认删除</div><p style="text-align:center;color:var(--text-2);margin-bottom:16px;">确定删除这个任务吗？</p><div style="display:flex;gap:10px;"><button class="btn btn-ghost btn-block" onclick="closeModal()">取消</button><button class="btn btn-danger btn-block" onclick="confirmDeleteTask(${id})">删除</button></div>`);}
function confirmDeleteTask(id){const today=DateUtil.today();let tasks=Store.getByDate('tasks',today)||[];tasks=tasks.filter(x=>x.id!==id);Store.setByDate('tasks',today,tasks);closeModal();toast('已删除');renderPage('tasks');}
function renderMath(){const chapters=Store.get('math_chapters',[]);let html=`<button class="btn btn-primary btn-block" onclick="addMathChapter()" style="margin-bottom:16px;">+ 新增章节</button>`;if(chapters.length===0){html+=`<div class="card"><div class="empty-state"><div class="empty-state-icon">📚</div><div class="empty-state-text">还没有章节<br>添加第一个章节开始记录吧</div></div></div>`;}else{html+='<div class="chapter-list">';chapters.forEach(ch=>{html+=`<div class="chapter-item ${ch.open?'open':''}" data-id="${ch.id}"><div class="chapter-header" onclick="toggleMathChapter(${ch.id})"><span class="chapter-name">${ch.mastered?'✅':'📘'} ${escapeHtml(ch.name)}</span><span class="chapter-toggle">▼</span></div><div class="chapter-body">${renderMathFields(ch)}</div></div>`;});html+='</div>';}return html;}
function renderMathFields(ch){const fields=[{key:'content',label:'今天学习内容',type:'textarea'},{key:'knowledge',label:'核心知识点',type:'textarea'},{key:'theorems',label:'重要定理',type:'textarea'},{key:'examples',label:'典型例题',type:'textarea'},{key:'stuck',label:'不会的地方',type:'textarea'},{key:'template',label:'解题模板',type:'textarea'},{key:'mistakes',label:'易错点',type:'textarea'},{key:'review2',label:'二刷时间',type:'date'},{key:'review3',label:'三刷时间',type:'date'}];return fields.map(f=>`<div class="field-group"><label class="field-label">${f.label}</label>${f.type==='textarea'?`<textarea class="textarea" data-ch="${ch.id}" data-field="${f.key}" placeholder="记录${f.label}...">${escapeHtml(ch[f.key]||'')}</textarea>`:`<input type="date" class="input" data-ch="${ch.id}" data-field="${f.key}" value="${escapeHtml(ch[f.key]||'')}">`}</div>`).join('')+`<div class="field-group"><label class="field-label">📷 上传图片</label><div class="img-preview-list" id="mathImg_${ch.id}">${(ch.images||[]).map((src,i)=>`<div class="img-thumb"><img src="${src}" onclick="viewImage('${src}')"><button class="remove-img" onclick="removeMathImage(${ch.id},${i})">×</button></div>`).join('')}</div><label class="img-upload-area" style="display:block;margin-top:8px;">+ 点击上传图片<input type="file" accept="image/*" multiple style="display:none;" onchange="uploadMathImage(${ch.id}, this.files)"></label></div><div style="display:flex;align-items:center;justify-content:space-between;margin-top:12px;"><label style="display:flex;align-items:center;gap:8px;cursor:pointer;"><div class="task-checkbox ${ch.mastered?'checked':''}" onclick="toggleMathMastered(${ch.id})"></div><span style="font-size:14px;color:var(--text-1);">是否掌握</span></label><button class="btn btn-danger btn-sm" onclick="deleteMathChapter(${ch.id})">🗑 删除章节</button></div>`;}
function addMathChapter(){showModal(`<div class="modal-title">新增章节</div><div class="field-group"><label class="field-label">章节名称</label><input type="text" class="input" id="newChapterName" placeholder="例如：高等数学 - 极限"></div><div style="display:flex;gap:10px;margin-top:16px;"><button class="btn btn-ghost btn-block" onclick="closeModal()">取消</button><button class="btn btn-primary btn-block" onclick="saveMathChapter()">添加</button></div>`);$('#newChapterName').focus();}
function saveMathChapter(){const name=$('#newChapterName').value.trim();if(!name){toast('请输入章节名称');return;}const chapters=Store.get('math_chapters',[]);chapters.push({id:Date.now(),name,content:'',knowledge:'',theorems:'',examples:'',stuck:'',template:'',mistakes:'',review2:'',review3:'',mastered:false,images:[],open:true});Store.set('math_chapters',chapters);closeModal();toast('章节已添加 📚');renderPage('math');}
function toggleMathChapter(id){const chapters=Store.get('math_chapters',[]);const ch=chapters.find(x=>x.id===id);if(ch){ch.open=!ch.open;Store.set('math_chapters',chapters);renderPage('math');}}
function toggleMathMastered(id){const chapters=Store.get('math_chapters',[]);const ch=chapters.find(x=>x.id===id);if(ch){ch.mastered=!ch.mastered;Store.set('math_chapters',chapters);renderPage('math');toast(ch.mastered?'恭喜掌握！🎉':'已取消掌握标记');}}
function deleteMathChapter(id){showModal(`<div class="modal-title">确认删除</div><p style="text-align:center;color:var(--text-2);margin-bottom:16px;">删除后无法恢复，确定吗？</p><div style="display:flex;gap:10px;"><button class="btn btn-ghost btn-block" onclick="closeModal()">取消</button><button class="btn btn-danger btn-block" onclick="confirmDeleteMathChapter(${id})">删除</button></div>`);}
function confirmDeleteMathChapter(id){let chapters=Store.get('math_chapters',[]);chapters=chapters.filter(x=>x.id!==id);Store.set('math_chapters',chapters);closeModal();toast('已删除');renderPage('math');}
function uploadMathImage(chId,files){const chapters=Store.get('math_chapters',[]);const ch=chapters.find(x=>x.id===chId);if(!ch)return;if(!ch.images)ch.images=[];let processed=0;const total=files.length;Array.from(files).forEach(file=>{handleImageUpload(file,dataUrl=>{ch.images.push(dataUrl);processed++;if(processed===total){Store.set('math_chapters',chapters);renderPage('math');toast('图片已上传 📷');}});});}
function removeMathImage(chId,idx){const chapters=Store.get('math_chapters',[]);const ch=chapters.find(x=>x.id===chId);if(ch&&ch.images){ch.images.splice(idx,1);Store.set('math_chapters',chapters);renderPage('math');}}
function viewImage(src){showModal(`<img src="${src}" style="width:100%;border-radius:12px;" onclick="closeModal()">`);}
const ENG_MODULES=[{key:'word',label:'单词',icon:'🔤'},{key:'reading',label:'阅读',icon:'📖'},{key:'translation',label:'翻译',icon:'🔄'},{key:'writing',label:'作文',icon:'✍️'},{key:'cloze',label:'完形',icon:'🧩'},{key:'newtype',label:'新题型',icon:'🌟'}];
function renderEnglish(){const today=DateUtil.today();const eng=Store.getByDate('english',today)||{};let html=`<div class="card" style="text-align:center;"><div style="font-size:13px;color:var(--text-3);margin-bottom:4px;">今日日期</div><div style="font-size:16px;font-weight:600;color:var(--text-1);">${today}</div></div>`;ENG_MODULES.forEach(m=>{const data=eng[m.key]||{};html+=`<div class="card"><div class="card-title"><span class="accent-line"></span>${m.icon} ${m.label}</div><div class="field-group"><label class="field-label">今日完成内容</label><textarea class="textarea" data-mod="${m.key}" data-field="content" placeholder="记录今天${m.label}学习了什么...">${escapeHtml(data.content||'')}</textarea></div><div class="field-group"><label class="field-label">不会的单词</label><textarea class="textarea" data-mod="${m.key}" data-field="words" placeholder="遇到的生词...">${escapeHtml(data.words||'')}</textarea></div><div class="field-group"><label class="field-label">长难句</label><textarea class="textarea" data-mod="${m.key}" data-field="sentences" placeholder="记录长难句分析...">${escapeHtml(data.sentences||'')}</textarea></div><div class="field-group"><label class="field-label">错误原因</label><textarea class="textarea" data-mod="${m.key}" data-field="errors" placeholder="为什么做错了...">${escapeHtml(data.errors||'')}</textarea></div><div class="field-group"><label class="field-label">需要复习内容</label><textarea class="textarea" data-mod="${m.key}" data-field="review" placeholder="需要回头看的...">${escapeHtml(data.review||'')}</textarea></div><div class="field-group"><label class="field-label">今日总结</label><textarea class="textarea" data-mod="${m.key}" data-field="summary" placeholder="一句话总结...">${escapeHtml(data.summary||'')}</textarea></div></div>`;});html+=`<button class="btn btn-primary btn-block" onclick="saveEnglish()" style="position:sticky;bottom:0;">💾 保存今日英语记录</button><div style="height:8px;"></div>`;return html;}
function saveEnglish(){const today=DateUtil.today();const eng=Store.getByDate('english',today)||{};ENG_MODULES.forEach(m=>{if(!eng[m.key])eng[m.key]={};['content','words','sentences','errors','review','summary'].forEach(field=>{const el=$(`[data-mod="${m.key}"][data-field="${field}"]`);if(el)eng[m.key][field]=el.value;});});Store.setByDate('english',today,eng);toast('英语记录已保存 📖');}
function renderMistakes(){const mistakes=Store.get('mistakes',[]);const subjects=[...new Set(mistakes.map(m=>m.subject).filter(Boolean))];let html=`<button class="btn btn-primary btn-block" onclick="addMistake()" style="margin-bottom:16px;">+ 添加错题</button>`;if(mistakes.length>0){html+=`<div class="filter-row"><div class="filter-chip ${!window._mistakeFilter?'active':''}" onclick="filterMistake('')">全部 (${mistakes.length})</div>${subjects.map(s=>`<div class="filter-chip ${window._mistakeFilter===s?'active':''}" onclick="filterMistake('${escapeHtml(s)}')">${escapeHtml(s)} (${mistakes.filter(m=>m.subject===s).length})</div>`).join('')}</div>`;}const filtered=window._mistakeFilter?mistakes.filter(m=>m.subject===window._mistakeFilter):mistakes;if(filtered.length===0){html+=`<div class="card"><div class="empty-state"><div class="empty-state-icon">📝</div><div class="empty-state-text">${mistakes.length===0?'还没有错题记录':'该科目暂无错题'}<br>每道错题都是成长的星标</div></div></div>`;}else{html+=filtered.map((m,idx)=>`<div class="card"><div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px;"><div><span style="font-size:12px;color:var(--text-muted);">#${idx+1}</span><span style="font-size:15px;font-weight:600;margin-left:6px;">${escapeHtml(m.subject||'未分类')}</span></div><div style="display:flex;gap:6px;"><button class="btn btn-ghost btn-sm" onclick="editMistake(${m.id})">✏️</button><button class="btn btn-danger btn-sm" onclick="deleteMistake(${m.id})">🗑</button></div></div><div style="font-size:13px;color:var(--text-2);line-height:1.7;">${m.knowledge?`<div><b>知识点：</b>${escapeHtml(m.knowledge)}</div>`:''}${m.errorReason?`<div><b>错误原因：</b>${escapeHtml(m.errorReason)}</div>`:''}${m.correctApproach?`<div><b>正确思路：</b>${escapeHtml(m.correctApproach)}</div>`:''}${m.template?`<div><b>解题模板：</b>${escapeHtml(m.template)}</div>`:''}${m.pitfall?`<div><b>容易踩坑：</b>${escapeHtml(m.pitfall)}</div>`:''}${m.reviewDate?`<div><b>复习时间：</b>${escapeHtml(m.reviewDate)}</div>`:''}${m.mastery?`<div><b>掌握情况：</b>${escapeHtml(m.mastery)}</div>`:''}</div>${(m.images&&m.images.length>0)?`<div class="img-preview-list" style="margin-top:10px;">${m.images.map(src=>`<div class="img-thumb"><img src="${src}" onclick="viewImage('${src}')"></div>`).join('')}</div>`:''}</div>`).join('');}return html;}
function filterMistake(subject){window._mistakeFilter=subject||null;renderPage('mistakes');}
function addMistake(){editMistake(null);}
function editMistake(id){const mistakes=Store.get('mistakes',[]);const m=id?mistakes.find(x=>x.id===id):{id:Date.now(),subject:'',knowledge:'',errorReason:'',correctApproach:'',template:'',pitfall:'',reviewDate:'',mastery:'',images:[]};if(!m)return;showModal(`<div class="modal-title">${id?'编辑错题':'添加错题'}</div><div class="field-group"><label class="field-label">所属科目</label><input type="text" class="input" id="m_subject" value="${escapeHtml(m.subject)}" placeholder="数学/英语/..."></div><div class="field-group"><label class="field-label">知识点</label><input type="text" class="input" id="m_knowledge" value="${escapeHtml(m.knowledge)}" placeholder="如：极限的计算"></div><div class="field-group"><label class="field-label">错误原因</label><textarea class="textarea" id="m_errorReason" placeholder="为什么做错了...">${escapeHtml(m.errorReason)}</textarea></div><div class="field-group"><label class="field-label">正确思路</label><textarea class="textarea" id="m_correctApproach" placeholder="应该怎么想...">${escapeHtml(m.correctApproach)}</textarea></div><div class="field-group"><label class="field-label">解题模板</label><textarea class="textarea" id="m_template" placeholder="这类题的通用解法...">${escapeHtml(m.template)}</textarea></div><div class="field-group"><label class="field-label">容易踩坑</label><textarea class="textarea" id="m_pitfall" placeholder="需要注意的坑...">${escapeHtml(m.pitfall)}</textarea></div><div class="field-group"><label class="field-label">再次复习时间</label><input type="date" class="input" id="m_reviewDate" value="${escapeHtml(m.reviewDate)}"></div><div class="field-group"><label class="field-label">掌握情况</label><select class="select" id="m_mastery"><option value="">请选择</option><option value="未掌握" ${m.mastery==='未掌握'?'selected':''}>未掌握</option><option value="部分掌握" ${m.mastery==='部分掌握'?'selected':''}>部分掌握</option><option value="基本掌握" ${m.mastery==='基本掌握'?'selected':''}>基本掌握</option><option value="完全掌握" ${m.mastery==='完全掌握'?'selected':''}>完全掌握</option></select></div><div class="field-group"><label class="field-label">📷 题目截图</label><div class="img-preview-list" id="mistakeImgList">${(m.images||[]).map((src,i)=>`<div class="img-thumb" data-idx="${i}"><img src="${src}" onclick="viewImage('${src}')"><button class="remove-img" onclick="this.parentElement.remove();window._mistakeImgs.splice(${i},1)">×</button></div>`).join('')}</div><label class="img-upload-area" style="display:block;margin-top:8px;">+ 上传图片<input type="file" accept="image/*" multiple style="display:none;" onchange="uploadMistakeImage(this.files)"></label></div><div style="display:flex;gap:10px;margin-top:16px;"><button class="btn btn-ghost btn-block" onclick="closeModal()">取消</button><button class="btn btn-primary btn-block" onclick="saveMistake(${id||'null'})">保存</button></div>`);window._mistakeImgs=[...(m.images||[])];}
function uploadMistakeImage(files){Array.from(files).forEach(file=>{handleImageUpload(file,dataUrl=>{window._mistakeImgs.push(dataUrl);const list=$('#mistakeImgList');const idx=window._mistakeImgs.length-1;const div=document.createElement('div');div.className='img-thumb';div.innerHTML=`<img src="${dataUrl}" onclick="viewImage('${dataUrl}')"><button class="remove-img" onclick="this.parentElement.remove();window._mistakeImgs.splice(${idx},1)">×</button>`;list.appendChild(div);});});}
function saveMistake(id){const data={subject:$('#m_subject').value.trim(),knowledge:$('#m_knowledge').value.trim(),errorReason:$('#m_errorReason').value.trim(),correctApproach:$('#m_correctApproach').value.trim(),template:$('#m_template').value.trim(),pitfall:$('#m_pitfall').value.trim(),reviewDate:$('#m_reviewDate').
function renderTimeline(){const today=DateUtil.today();const todayTimelines=Store.getByDate('timelines',today)||[];const weekDates=DateUtil.weekDates();let weekMinutes=0,mathMinutes=0,engMinutes=0,pomodoroCount=0;weekDates.forEach(d=>{const tls=Store.getByDate('timelines',d)||[];tls.forEach(t=>{weekMinutes+=t.duration||0;if(t.subject==='math')mathMinutes+=t.duration||0;if(t.subject==='english')engMinutes+=t.duration||0;pomodoroCount+=t.pomodoros||0;});});const todayMinutes=todayTimelines.reduce((s,t)=>s+(t.duration||0),0);const streakDays=DateUtil.streak('timelines');let html=`<div class="stat-row"><div class="stat-mini"><div class="stat-mini-value">${(todayMinutes/60).toFixed(1)}h</div><div class="stat-mini-label">今日时长</div></div><div class="stat-mini"><div class="stat-mini-value">${(weekMinutes/60).toFixed(1)}h</div><div class="stat-mini-label">本周时长</div></div><div class="stat-mini"><div class="stat-mini-value">${streakDays}</div><div class="stat-mini-label">连续天数</div></div></div><div class="stat-row"><div class="stat-mini"><div class="stat-mini-value">${(mathMinutes/60).toFixed(1)}h</div><div class="stat-mini-label">数学时长</div></div><div class="stat-mini"><div class="stat-mini-value">${(engMinutes/60).toFixed(1)}h</div><div class="stat-mini-label">英语时长</div></div><div class="stat-mini"><div class="stat-mini-value">${pomodoroCount}</div><div class="stat-mini-label">番茄钟数</div></div></div><button class="btn btn-primary btn-block" onclick="addTimeline()" style="margin-bottom:16px;">+ 记录一轮学习</button>`;if(todayTimelines.length===0){html+=`<div class="card"><div class="empty-state"><div class="empty-state-icon">📈</div><div class="empty-state-text">今天还没有学习记录<br>开始第一轮专注吧</div></div></div>`;}else{html+='<div class="card"><div class="card-title"><span class="accent-line"></span>今日时间轴</div><div class="timeline">';todayTimelines.forEach(t=>{const subs={math:'📚 数学',english:'📖 英语',other:'🌟 其他'};html+=`<div class="timeline-item"><div class="timeline-time">${escapeHtml(t.start||'')} - ${escapeHtml(t.end||'')} · ${subs[t.subject]||t.subject||''} · ${t.duration||0}分钟</div><div class="timeline-content"><div style="font-size:14px;font-weight:600;margin-bottom:4px;">${escapeHtml(t.content||'')}</div>${t.feeling?`<div style="font-size:12px;color:var(--text-3);">感受：${escapeHtml(t.feeling)}</div>`:''}<div style="display:flex;gap:10px;margin-top:4px;font-size:11px;color:var(--text-muted);">${t.focus?`<span>专注度 ${t.focus}/10</span>`:''}${t.pomodoros?`<span>🍅 ${t.pomodoros}个番茄钟</span>`:''}</div><button class="btn btn-danger btn-sm" onclick="deleteTimeline(${t.id})" style="margin-top:6px;">🗑 删除</button></div></div>`;});html+='</div></div>';}return html;}
function addTimeline(){const now=new Date();const timeNow=String(now.getHours()).padStart(2,'0')+':'+String(now.getMinutes()).padStart(2,'0');showModal(`<div class="modal-title">记录一轮学习</div><div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;"><div class="field-group"><label class="field-label">开始时间</label><input type="time" class="input" id="t_start" value="${timeNow}"></div><div class="field-group"><label class="field-label">结束时间</label><input type="time" class="input" id="t_end" value="${timeNow}"></div></div><div class="field-group"><label class="field-label">学习内容</label><input type="text" class="input" id="t_content" placeholder="例如：高数极限例题"></div><div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;"><div class="field-group"><label class="field-label">学习时长(分钟)</label><input type="number" class="input" id="t_duration" placeholder="60" value="0"></div><div class="field-group"><label class="field-label">科目</label><select class="select" id="t_subject"><option value="math">📚 数学</option><option value="english">📖 英语</option><option value="other">🌟 其他</option></select></div></div><div class="field-group"><label class="field-label">专注程度 (1-10)</label><div class="rating-row" id="focusPicker">${Array.from({length:10},(_,i)=>`<div class="rating-dot" data-val="${i+1}">${i+1}</div>`).join('')}</div></div><div class="field-group"><label class="field-label">🍅 番茄钟数量</label><input type="number" class="input" id="t_pomodoros" value="0" min="0"></div><div class="field-group"><label class="field-label">学习感受</label><textarea class="textarea" id="t_feeling" placeholder="这轮学习感觉如何..."></textarea></div><div style="display:flex;gap:10px;margin-top:16px;"><button class="btn btn-ghost btn-block" onclick="closeModal()">取消</button><button class="btn btn-primary btn-block" onclick="saveTimeline()">保存</button></div>`);let focusVal=5;$$('#focusPicker .rating-dot').forEach(el=>{if(parseInt(el.dataset.val)===focusVal)el.classList.add('active');el.addEventListener('click',()=>{$$('#focusPicker .rating-dot').forEach(x=>x.classList.remove('active'));el.classList.add('active');focusVal=parseInt(el.dataset.val);});});window._focusVal=()=>focusVal;$('#t_end').addEventListener('change',calcDuration);$('#t_start').addEventListener('change',calcDuration);}
function calcDuration(){const s=$('#t_start').value;const e=$('#t_end').value;if(s&&e){const[sh,sm]=s.split(':').map(Number);const[eh,em]=e.split(':').map(Number);let mins=(eh*60+em)-(sh*60+sm);if(mins<0)mins+=24*60;if(mins>0)$('#t_duration').value=mins;}}
function saveTimeline(){const data={id:Date.now(),start:$('#t_start').value,end:$('#t_end').value,content:$('#t_content').value.trim(),duration:parseInt($('#t_duration').value)||0,subject:$('#t_subject').value,focus:window._focusVal?window._focusVal():5,pomodoros:parseInt($('#t_pomodoros').value)||0,feeling:$('#t_feeling').value.trim()};if(!data.content){toast('请输入学习内容');return;}const today=DateUtil.today();const tls=Store.getByDate('timelines',today)||[];tls.push(data);Store.setByDate('timelines',today,tls);closeModal();toast('学习记录已保存 📈');renderPage('timeline');}
function deleteTimeline(id){const today=DateUtil.today();let tls=Store.getByDate('timelines',today)||[];tls=tls.filter(x=>x.id!==id);Store.setByDate('timelines',today,tls);toast('已删除');renderPage('timeline');}
function renderHealth(){const today=DateUtil.today();const h=Store.getByDate('health',today)||{};let html=`<div class="card" style="text-align:center;"><div style="font-size:13px;color:var(--text-3);">今日健康记录</div><div style="font-size:16px;font-weight:600;color:var(--text-1);margin-top:4px;">${today}</div></div><div class="card"><div class="card-title"><span class="accent-line"></span>😴 睡眠</div><div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;"><div class="field-group"><label class="field-label">睡眠时间</label><input type="time" class="input" id="h_sleepTime" value="${escapeHtml(h.sleepTime)}"></div><div class="field-group"><label class="field-label">起床时间</label><input type="time" class="input" id="h_wakeTime" value="${escapeHtml(h.wakeTime)}"></div></div></div><div class="card"><div class="card-title"><span class="accent-line"></span>🌟 精神状态</div><div class="field-group"><label class="field-label">精神状态 (1-10)</label><div class="rating-row" id="spiritPicker">${Array.from({length:10},(_,i)=>`<div class="rating-dot ${i+1===(h.spirit||5)?'active':''}" data-val="${i+1}">${i+1}</div>`).join('')}</div></div><div class="field-group"><label class="field-label">压力指数 (1-10)</label><div class="rating-row" id="stressPicker">${Array.from({length:10},(_,i)=>`<div class="rating-dot ${i+1===(h.stress||3)?'active':''}" data-val="${i+1}">${i+1}</div>`).join('')}</div></div></div><div class="card"><div class="card-title"><span class="accent-line"></span>💧 喝水量</div><div class="field-group"><label class="field-label">今日喝水（杯）</label><input type="number" class="input" id="h_water" value="${h.water||0}" min="0"></div></div><div class="card"><div class="card-title"><span class="accent-line"></span>🏃 身体状态</div><div class="field-group"><label class="field-label">身体状态</label><select class="select" id="h_bodyState"><option value="">请选择</option>${['很好','良好','一般','疲惫','不适'].map(s=>`<option value="${s}" ${h.bodyState===s?'selected':''}>${s}</option>`).join('')}</select></div></div><div class="card"><div class="card-title"><span class="accent-line"></span>🌸 今日心情</div><div class="field-group"><label class="field-label">心情</label><div class="mood-row" id="moodPicker">${['😊 开心','😌 平静','😴 疲惫','😰 焦虑','😢 低落','😤 有压力','🥰 满足'].map(m=>`<div class="mood-btn ${h.mood===m?'active':''}" data-mood="${m}">${m}</div>`).join('')}</div></div></div><button class="btn btn-primary btn-block" onclick="saveHealth()">💾 保存今日健康记录</button><div style="height:8px;"></div>`;return html;}
function saveHealth(){const today=DateUtil.today();const data={sleepTime:$('#h_sleepTime').value,wakeTime:$('#h_wakeTime').value,spirit:window._spiritVal?window._spiritVal():5,stress:window._stressVal?window._stressVal():3,water:parseInt($('#h_water').value)||0,bodyState:$('#h_bodyState').value,mood:window._moodVal||''};Store.setByDate('health',today,data);toast('健康记录已保存 🌱');}
function renderExercise(){const today=DateUtil.today();const todayEx=Store.getByDate('exercise',today)||[];const weekDates=DateUtil.weekDates();let weekCount=0,totalMinutes=0;weekDates.forEach(d=>{const ex=Store.getByDate('exercise',d)||[];weekCount+=ex.length;ex.forEach(e=>totalMinutes+=e.duration||0);});let html=`<div class="stat-row"><div class="stat-mini"><div class="stat-mini-value">${weekCount}</div><div class="stat-mini-label">本周运动次数</div></div><div class="stat-mini"><div class="stat-mini-value">${(totalMinutes/60).toFixed(1)}h</div><div class
function renderDaily(){const today=DateUtil.today();const d=Store.getByDate('daily_review',today)||{};const fields=[{key:'done',label:'🌟 今天完成了什么',ph:'今天完成了哪些事情...'},{key:'gain',label:'🌟 今天最大的收获',ph:'今天学到了什么、感悟了什么...'},{key:'problem',label:'🌟 今天遇到的问题',ph:'遇到了什么困难，怎么解决的...'},{key:'happy',label:'🌟 今天最值得开心的一件事',ph:'哪怕很小的事也值得记录...'},{key:'tomorrow',label:'🌟 明天最重要的三件事',ph:'1. ...\n2. ...\n3. ...'},{key:'letter',label:'🌟 给未来的自己一句话',ph:'想对未来的自己说什么...'}];let html=`<div class="card" style="text-align:center;background:linear-gradient(135deg,rgba(184,169,217,0.2),rgba(212,185,106,0.1));"><div style="font-size:13px;color:var(--text-3);">每日复盘</div><div style="font-size:18px;font-weight:700;color:var(--text-1);margin-top:4px;">${today} ${DateUtil.weekday(DateUtil.parse(today))}</div></div>`;fields.forEach(f=>{html+=`<div class="card"><div class="card-title"><span class="accent-line"></span>${f.label}</div><textarea class="textarea" data-review="${f.key}" placeholder="${f.ph}" style="min-height:90px;">${escapeHtml(d[f.key]||'')}</textarea></div>`;});html+=`<div class="card"><div class="card-title"><span class="accent-line"></span>🌟 今日评分（100分）</div><div style="display:flex;align-items:center;gap:12px;"><input type="range" min="0" max="100" value="${d.score||75}" class="input" id="reviewScore" style="padding:0;flex:1;" oninput="$('#scoreDisplay').textContent=this.value"><div style="font-size:24px;font-weight:700;color:var(--lav-700);min-width:60px;text-align:center;" id="scoreDisplay">${d.score||75}</div></div></div><button class="btn btn-primary btn-block" onclick="saveDailyReview()">💾 保存今日复盘</button><div style="height:8px;"></div><div class="history-section"><div class="history-title">📖 历史复盘</div>${renderDailyHistory()}</div>`;return html;}
function renderDailyHistory(){const all=Store.get('daily_review',{});const dates=Object.keys(all).sort().reverse().filter(d=>d!==DateUtil.today());if(dates.length===0){return '<div class="empty-state" style="padding:20px;"><div class="empty-state-text">暂无历史复盘</div></div>';}return dates.slice(0,30).map(d=>{const r=all[d];return `<div class="history-item" onclick="viewDailyHistory('${d}')"><div class="history-date">${d} ${DateUtil.weekday(DateUtil.parse(d))} · 评分 ${r.score||'-'}</div><div class="history-content">${escapeHtml((r.done||'').slice(0,60))}${(r.done||'').length>60?'...':''}</div></div>`;}).join('');}
function viewDailyHistory(date){const r=Store.getByDate('daily_review',date)||{};showModal(`<div class="modal-title">📖 ${date} 的复盘</div><div style="max-height:60vh;overflow-y:auto;">${r.done?`<div style="margin-bottom:12px;"><b>🌟 完成了什么：</b><br>${escapeHtml(r.done)}</div>`:''}${r.gain?`<div style="margin-bottom:12px;"><b>🌟 最大收获：</b><br>${escapeHtml(r.gain)}</div>`:''}${r.problem?`<div style="margin-bottom:12px;"><b>🌟 遇到的问题：</b><br>${escapeHtml(r.problem)}</div>`:''}${r.happy?`<div style="margin-bottom:12px;"><b>🌟 开心的事：</b><br>${escapeHtml(r.happy)}</div>`:''}${r.tomorrow?`<div style="margin-bottom:12px;"><b>🌟 明天三件事：</b><br>${escapeHtml(r.tomorrow).replace(/\n/g,'<br>')}</div>`:''}${r.letter?`<div style="margin-bottom:12px;"><b>🌟 给未来的自己：</b><br>${escapeHtml(r.letter)}</div>`:''}<div style="text-align:center;margin-top:16px;"><b style="font-size:20px;color:var(--lav-600);">${r.score||'-'} 分</b></div></div><button class="btn btn-ghost btn-block" onclick="closeModal()" style="margin-top:16px;">关闭</button>`);}
function saveDailyReview(){const today=DateUtil.today();const d=Store.getByDate('daily_review',today)||{};$$('[data-review]').forEach(el=>{d[el.dataset.review]=el.value;});d.score=parseInt($('#reviewScore').value)||0;Store.setByDate('daily_review',today,d);if(d.tomorrow){const lines=d.tomorrow.split('\n').map(s=>s.replace(/^\d+\.\s*/,'').trim()).filter(s=>s).slice(0,3);Store.set('top3_'+today,lines);}toast('复盘已保存 🌙');}
function renderWeekly(){const weekKey=DateUtil.weekKey();const weekLabel=DateUtil.weekRangeLabel();const w=Store.getByDate('weekly_review',weekKey)||{};const fields=[{key:'done',label:'✨ 本周完成情况',ph:'这周完成了哪些重要的事...'},{key:'math',label:'📚 本周数学进度',ph:'数学学了哪些章节，掌握如何...'},{key:'english',label:'📖 本周英语进度',ph:'英语各模块完成情况...'},{key:'health',label:'🌱 本周身体状态',ph:'睡眠、运动、饮食整体如何...'},{key:'growth',label:'⭐ 本周最大的成长',ph:'这周自己有什么进步和感悟...'},{key:'next',label:'🚀 下周目标',ph:'下周最重要的几件事...'}];let html=`<div class="card" style="text-align:center;background:linear-gradient(135deg,rgba(184,169,217,0.2),rgba(212,185,106,0.1));"><div style="font-size:13px;color:var(--text-3);">🌌 每周复盘</div><div style="font-size:16px;font-weight:700;color:var(--text-1);margin-top:4px;">${weekLabel}</div><div style="font-size:11px;color:var(--text-muted);margin-top:4px;">${weekKey}</div></div>`;fields.forEach(f=>{html+=`<div class="card"><div class="card-title"><span class="accent-line"></span>${f.label}</div><textarea class="textarea" data-weekly="${f.key}" placeholder="${f.ph}" style="min-height:100px;">${escapeHtml(w[f.key]||'')}</textarea></div>`;});html+=`<button class="btn btn-primary btn-block" onclick="saveWeeklyReview()">💾 保存本周复盘</button><div style="height:8px;"></div><div class="history-section"><div class="history-title">🌌 历史周报</div>${renderWeeklyHistory(weekKey)}</div>`;return html;}
function renderWeeklyHistory(currentWeekKey){const all=Store.get('weekly_review',{});const weeks=Object.keys(all).sort().reverse().filter(w=>w!==currentWeekKey);if(weeks.length===0){return '<div class="empty-state" style="padding:20px;"><div class="empty-state-text">暂无历史周报</div></div>';}return weeks.map(wk=>{const r=all[wk];return `<div class="history-item" onclick="viewWeeklyHistory('${wk}')"><div class="history-date">${wk}</div><div class="history-content">${escapeHtml((r.growth||r.done||'').slice(0,60))}${(r.growth||r.done||'').length>60?'...':''}</div></div>`;}).join('');}
function viewWeeklyHistory(weekKey){const r=Store.getByDate('weekly_review',weekKey)||{};showModal(`<div class="modal-title">🌌 ${weekKey} 周报</div><div style="max-height:60vh;overflow-y:auto;">${r.done?`<div style="margin-bottom:12px;"><b>✨ 完成情况：</b><br>${escapeHtml(r.done)}</div>`:''}${r.math?`<div style="margin-bottom:12px;"><b>📚 数学进度：</b><br>${escapeHtml(r.math)}</div>`:''}${r.english?`<div style="margin-bottom:12px;"><b>📖 英语进度：</b><br>${escapeHtml(r.english)}</div>`:''}${r.health?`<div style="margin-bottom:12px;"><b>🌱 身体状态：</b><br>${escapeHtml(r.health)}</div>`:''}${r.growth?`<div style="margin-bottom:12px;"><b>⭐ 最大成长：</b><br>${escapeHtml(r.growth)}</div>`:''}${r.next?`<div style="margin-bottom:12px;"><b>🚀 下周目标：</b><br>${escapeHtml(r.next)}</div>`:''}</div><button class="btn btn-ghost btn-block" onclick="closeModal()" style="margin-top:16px;">关闭</button>`);}
function saveWeeklyReview(){const weekKey=DateUtil.weekKey();const w=Store.getByDate('weekly_review',weekKey)||{};$$('[data-weekly]').forEach(el=>{w[el.dataset.weekly]=el.value;});Store.setByDate('weekly_review',weekKey,w);toast('周报已保存 🌌');}
function bindPageEvents(pageId){if(pageId==='health'){let spiritVal=5,stressVal=3;const h=Store.getByDate('health',DateUtil.today())||{};spiritVal=h.spirit||5;stressVal=h.stress||3;$$('#spiritPicker .rating-dot').forEach(el=>{el.addEventListener('click',()=>{$$('#spiritPicker .rating-dot').forEach(x=>x.classList.remove('active'));el.classList.add('active');spiritVal=parseInt(el.dataset.val);});});$$('#stressPicker .rating-dot').forEach(el=>{el.addEventListener('click',()=>{$$('#stressPicker .rating-dot').forEach(x=>x.classList.remove('active'));el.classList.add('active');stressVal=parseInt(el.dataset.val);});});window._spiritVal=()=>spiritVal;window._stressVal=()=>stressVal;let moodVal=h.mood||'';$$('#moodPicker .mood-btn').forEach(el=>{el.addEventListener('click',()=>{$$('#moodPicker .mood-btn').forEach(x=>x.classList.remove('active'));el.classList.add('active');moodVal=el.dataset.mood;});});window._moodVal=()=>moodVal;}if(pageId==='math'){$$('[data-ch][data-field]').forEach(el=>{el.addEventListener('change',()=>{const chId=parseInt(el.dataset.ch);const field=el.dataset.field;const chapters=Store.get('math_chapters',[]);const ch=chapters.find(x=>x.id===chId);if(ch){ch[field]=el.value;Store.set('math_chapters',chapters);}});});}if(pageId==='english'){$$('[data-mod][data-field]').forEach(el=>{el.addEventListener('change',()=>{const today=DateUtil.today();const eng=Store.getByDate('english',today)||{};if(!eng[el.dataset.mod])eng[el.dataset.mod]={};eng[el.dataset.mod][el.dataset.field]=el.value;Store.setByDate('english',today,eng);});});}if(pageId==='diet'){$$('[data-diet]').forEach(el=>{el.addEventListener('change',()=>{const today=DateUtil.today();const d=Store.getByDate('diet',today)||{};d[el.dataset.diet]=el.value;Store.setByDate('diet',today,d);});});}if(pageId==='daily'){$$('[data-review]').forEach(el=>{el.addEventListener('change',()=>{const today=DateUtil.today();const d=Store.getByDate('daily_review',today)||{};d[el.dataset.review]=el.value;Store.setByDate('daily_review',today,d);});});}if(pageId==='weekly'){$$('[data-weekly]').forEach(el=>{el.addEventListener('change',()=>{const weekKey=DateUtil.weekKey();const w=Store.getByDate('weekly_review',weekKey)||{};w[el.dataset.weekly]=el.value;Store.setByDate('weekly_review',weekKey,w);});});}}
function init(){generateStars();renderNav();renderPage('dashboard');if(window.twemoji){try{twemoji.parse(document.body,{folder:'svg',ext:'.svg',base:'https://cdn.jsdelivr.net/gh/twitter/twemoji@v14.0.2/assets/'});}catch(e){}}if('serviceWorker' in navigator){navigator.serviceWorker.register('sw.js').catch(()=>{});}let lastTouch=0;document.addEventListener('touchend',e=>{const now=Date.now();if(now-lastTouch<=300)e.preventDefault();lastTouch=now;},{passive:false});}
window.navigateTo=navigateTo;window.editExamDate=editExamDate;window.saveExamDate=saveExamDate;window.toggleTask=toggleTask;window.addTaskModal=addTaskModal;window.saveNewTask=saveNewTask;window.editTask=editTask;window.saveEditTask=saveEditTask;window.deleteTask=deleteTask;window.confirmDeleteTask=confirmDeleteTask;window.addMathChapter=addMathChapter;window.saveMathChapter=saveMathChapter;window.toggleMathChapter=toggleMathChapter;window.toggleMathMastered=toggleMathMastered;window.deleteMathChapter=deleteMathChapter;window.confirmDeleteMathChapter=confirmDeleteMathChapter;window.uploadMathImage=uploadMathImage;window.removeMathImage=removeMathImage;window.viewImage=viewImage;window.saveEnglish=saveEnglish;window.filterMistake=filterMistake;window.addMistake=addMistake;window.editMistake=editMistake;window.uploadMistakeImage=uploadMistakeImage;window.saveMistake=saveMistake;window.deleteMistake=deleteMistake;window.confirmDeleteMistake=confirmDeleteMistake;window.addTimeline=addTimeline;window.calcDuration=calcDuration;window.saveTimeline=saveTimeline;window.deleteTimeline=deleteTimeline;window.saveHealth=saveHealth;window.addExercise=addExercise;window.saveExercise=saveExercise;window.deleteExercise=deleteExercise;window.adjustWater=adjustWater;window.saveDiet=saveDiet;window.saveDailyReview=saveDailyReview;window.viewDailyHistory=viewDailyHistory;window.saveWeeklyReview=saveWeeklyReview;window.viewWeeklyHistory=viewWeeklyHistory;window.closeModal=closeModal;document.addEventListener('DOMContentLoaded',init);


